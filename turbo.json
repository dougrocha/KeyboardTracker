{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "web#build": {
      "dependsOn": ["^build", "$NEXT_PUBLIC_API_URL", "$NEXT_PUBLIC_WEB_URL"],
      "outputs": [".next/**"]
    }
  },
  "globalDependencies": ["$NODE_ENV"]
}
